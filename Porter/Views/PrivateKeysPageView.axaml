<UserControl xmlns="https://github.com/avaloniaui"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:vm="using:Porter.ViewModels"
			 xmlns:c="using:Porter.Controls"
			 xmlns:s="using:Myth.Avalonia.Services"
			 mc:Ignorable="d" d:DesignWidth="500" d:DesignHeight="450"
			 s:DialogManager.Register="{Binding}"
			 x:DataType="vm:PrivateKeysPageViewModel"
			 x:Class="Porter.Views.PrivateKeysPageView">
	
	<Grid RowDefinitions="Auto,10,Auto">
		<Grid Grid.Row="0" 
			  ColumnDefinitions="Auto,*,Auto"
			  Height="30">
			<Button Grid.Column="0"
					Classes="no-padding content-center"
					Command="{Binding GoToTunnelsCommand}"
					Height="{Binding $parent.Height }"
					Width="{Binding $self.Height}">
				<TextBlock
					Text="&#xE138;"
					FontSize="20"
					FontFamily="{ StaticResource Phosphor }"/>
			</Button>
			<TextBlock Grid.Column="1" 
					   Text="Private Keys"
					   VerticalAlignment="Center"
					   HorizontalAlignment="Center"
					   FontSize="16"/>
			<Button Grid.Column="2"
					Classes="no-padding content-center"
					ToolTip.Tip="Add new Private Key"
					Command="{Binding AddPrivateKey}"
					Height="{Binding $parent.Height }"
					Width="{Binding $self.Height}">
				<TextBlock
					Text="&#xE3D4;"
					FontSize="20"
					FontFamily="{ StaticResource Phosphor }"/>
			</Button>
		</Grid>

		<ScrollViewer Grid.Row="2">
			<ItemsControl ItemsSource="{Binding Items}">
				<ItemsControl.ItemsPanel>
					<ItemsPanelTemplate>
						<StackPanel Spacing="5" />
					</ItemsPanelTemplate>
				</ItemsControl.ItemsPanel>

				<ItemsControl.ItemTemplate>
					<DataTemplate>
						<c:PrivateKeyControl Padding="5"
											 Background="#2E2E2E"
											 CornerRadius="5">
						</c:PrivateKeyControl>
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>
		</ScrollViewer>

	</Grid>
</UserControl>
